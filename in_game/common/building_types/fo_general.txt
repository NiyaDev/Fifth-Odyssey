
overforage = {
  is_foreign = no
  pop_type = peasants
  max_levels = 1

  category = village_category
  employment_size = 1

  rural_settlement = no
  town = yes
  city = no
  allow = {
    NOT = { has_variable = settled }
    tag = c:FO0
  }
  remove_if = { has_variable = settled }
  build_time = 10

  modifier = {
    local_monthly_food = 6.0
    local_peasants_estate_power = 1.0
    local_monthly_prosperity = -0.005
  }
}

