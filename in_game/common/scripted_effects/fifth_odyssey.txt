
update_situation_pops = {
  c:EXO = {
    set_local_variable = {
      name = nobles
      value = {
        add = num_pop_type_in_country:nobles
        multiply = 1000
      }
    }
    set_local_variable = {
      name = clergy
      value = {
        add = num_pop_type_in_country:clergy
        multiply = 1000
      }
    }
    set_local_variable = {
      name = burghers
      value = {
        add = num_pop_type_in_country:burghers
        multiply = 1000
      }
    }
    set_local_variable = {
      name = commoners
      value = {
        add = num_pop_type_in_country:peasants
        add = num_pop_type_in_country:laborers
        multiply = 1000
      }
    }
  }
  situation:fo_fifth_odyssey = {
    set_variable = {
      name = nobles_count
      value = local_var:nobles
    }
    set_variable = {
      name = clergy_count
      value = local_var:clergy
    }
    set_variable = {
      name = burghers_count
      value = local_var:burghers
    }
    set_variable = {
      name = commoners_count
      value = local_var:commoners
    }
  }
}

